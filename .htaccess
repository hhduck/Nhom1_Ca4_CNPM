# Enable rewrite engine
RewriteEngine On

# Set the default file to serve when a directory is requested
DirectoryIndex pages/home/home.html

# Rule to internally rewrite the root URL to the home page
# This handles requests for just "http://localhost/"
RewriteRule ^$ /pages/home/home.html [L]